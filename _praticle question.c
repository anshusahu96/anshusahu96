#include<stdio.h>
    int main(){
        // Compute rectangle perimeter
        int height , width;
        height = 7;
        width = 5;
        int perimeter;
        perimeter= height*width;
        printf(" perimeter of the rectangle= %d icnhes\n", perimeter);
        int area;
        area = 2*(height + width);
        printf("Area of the rectangle= %d squre inches\n\n", area);
        // compute ciecle perimeter and area 
        int redius, pi;
        redius = scanf("%f", &redius);
        pi = 3.14;
        int perimeter, area;
        perimeer = 2*pi*redius;
        area = pi*redius*redius;
        printf("Perimeter of the Circle= %f inches\n");
        printf("Area of the Circle= %f squre inches\n");
        
         
        
        
    }